<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.diversite.mapper.productAttribute.ProductAttributeMapper">

    <select id="getAllProductAttributes" resultType="com.diversite.entity.address.AddressEntity">
        SELECT pa.product_id,a.id, a.name, av.value
        FROM product_attribute pa
                 LEFT JOIN attributes a
                           ON pa.attribute_id=a.id
                 LEFT JOIN attribute_values av
                           ON av.attribute_id=a.id
        WHERE product_id = #{id};
    </select>



</mapper>
