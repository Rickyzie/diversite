<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.diversite.mapper.user.UserMapper">

    <!-- Insert User -->
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (name, email, password_hash, phone_number, address)
        VALUES (#{name}, #{email}, #{passwordHash}, #{phoneNumber}, #{address})
    </insert>

    <!-- Select User by ID -->
    <select id="getUserById" resultType="com.diversite.entity.UserEntity">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <!-- Select All Users -->
    <select id="getAllUsers" resultType="com.diversite.entity.UserEntity">
        SELECT * FROM users
    </select>

    <!-- Update User -->
    <update id="updateUser">
        UPDATE users
        SET name = #{name},
            email = #{email},
            password_hash = #{passwordHash},
            phone_number = #{phoneNumber},
            address = #{address},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- Delete User -->
    <delete id="deleteUser">
        DELETE FROM public.users WHERE id = #{id}
    </delete>

</mapper>

